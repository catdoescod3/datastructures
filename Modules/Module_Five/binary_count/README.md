- Compile the test suite with `g++ -std=c++23 binary_tree.cpp main.cpp -o
  binary_test_suite`
- Run the test suite with `./binary_test_suite`. The trees are generated
  randomly each run so the outputs should vary.
